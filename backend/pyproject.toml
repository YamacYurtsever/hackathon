[tool.vulture]
min_confidence = 80
paths = [
  "app.py",
  "ir_models.py",
  "mistral_service.py",
  "seed.py",
  "storage.py",
  ".vulture_whitelist.py",
]

[tool.pytest.ini_options]
pythonpath = ["."]
